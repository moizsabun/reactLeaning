import React from "react";


function Food(props)
{
    return (

    <div><h2>Today we have in Dinner {props.Dinner}  and in sweet Dish we have {props.Sweet}</h2> </div>
    );
}

export default Food;